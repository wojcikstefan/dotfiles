[user]
    name = Stefan Wojcik
    email = wojcikstefan@gmail.com

[color]
    ui = true
    pager = true

[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green

[color "diff"]
    meta = yellow bold
    frag = magenta
    old = red
    new = green

[color "diff-highlight"]
    oldNormal = red bold
    oldHighlight = red bold 52
    newNormal = green bold
    newHighlight = green bold 22

[color "status"]
    added = yellow
    changed = green
    untracked = cyan

[core]
    # pager = less -FRSX
    # using https://github.com/stevemao/diff-so-fancy for diffs
    pager = diff-highlight | diff-so-fancy | less --tabs=1,5 -FRSX
    whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol
    excludesfile = ~/.gitignore
    quotepath = false
    autocrlf = input
    editor = vim

[alias]
    st = status
    stat = status
    ci = commit
    ic = commit
    cia = commit -a
    cp = cherry-pick
    a = add
    d = diff
    dc = diff --cached
    rb = rebase
    rbpus = !"git pull --rebase && git pus"
    br = branch
    co = "!f(){ git checkout \"$@\" && git submodule update --init --recursive --quiet; }; f"
    ch = checkout
    lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
    unstage = reset HEAD
    subdate = submodule update --init --recursive --quiet
    sub = submodule update --init --recursive --quiet
    ca = commit --amend
    fix = commit -a --amend -C HEAD
    up = !sh -c 'git pull --rebase --prune && git log --pretty=format:\"%Cred%ae %Creset- %C(yellow)%s %Creset(%ar)\" HEAD@{1}..'
    mt = mergetool
    m = merge --no-ff
    pul = pull
    pus = push
    pushstaging = !sh -c 'git branch -D staging && git checkout -b staging && git push -f -u origin staging'

    stup = !"git stash && git pl && git ps && git stash pop"
    ps = !"git push origin `git branch | sed -e '/^[^*]/d' -e 's/* \\(.*\\)/\\1/'`"
    pl = !"git pull --rebase origin `git branch | sed -e '/^[^*]/d' -e 's/* \\(.*\\)/\\1/'`"
    latestbr = !"git for-each-ref --sort=-committerdate refs/heads/ | head"

    today = "log --since=midnight --author='Stefan Wojcik' --oneline --all --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'"
    yesterday = "log --date=relative --since='2 days ago' --until=midnight --author='Stefan Wojcik' --oneline --all --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'"
    fix = commit -a --amend -C HEAD

[merge]
    tool = opendiff

[push]
    default = simple
